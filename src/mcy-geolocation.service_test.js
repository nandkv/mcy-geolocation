'use strict';

describe('mcyGeolocation module', function () {
  beforeEach(module('mcyGeolocation'));
  beforeEach(function () {
 
  });

  describe('copy-text directive', function () {
    it('should print current version', function () {
    });
  });
});
